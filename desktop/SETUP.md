# Olisar desktop app — setup

The setup guide now lives at the repository root: **[SETUP.md](../SETUP.md)**.

It covers installing the desktop app (macOS + Windows), creating your Discord application,
the first-run wizard, optional remote access over Tailscale Funnel, where your data lives,
troubleshooting, and building from source.
